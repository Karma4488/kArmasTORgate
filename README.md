# kArmasTORgate

sudo apt install tor -y
sudo python3 kArmasTORgate.py start    # route everything through Tor
python3 kArmasTORgate.py status        # confirm exit IP via check.torproject.org
sudo python3 kArmasTORgate.py stop     # restore normal networking

run it with sudo
